# m7 — Training the AIFS-ENS with Anemoi (CRPS ensemble training)
# Verified end-to-end on a single NVIDIA A100 (40 GB), Python 3.11, CUDA 12.4.

anemoi-training==0.6.3
anemoi-models==0.9.3
anemoi-datasets==0.5.26
# earthkit-data must stay <1.0: v1.0 renamed normalize_order_by -> normalise_order_by,
# which breaks anemoi-datasets 0.5.26's `create` pipeline (Part 1, building the local zarr).
earthkit-data==0.20.0
anemoi-graphs==0.6.3
anemoi-utils==0.4.35
anemoi-transform==0.1.15
torch==2.6.0
torchvision==0.21.0
torch-geometric==2.6.1
pytorch-lightning==2.5.2
torchmetrics==1.8.0
hydra-core==1.3.2
omegaconf==2.3.0
mlflow==3.3.1
numpy==1.26.4
matplotlib
pandas
# Part 3 (inference on current weather from ECMWF open data)
anemoi-inference==0.8.3
ecmwf-opendata==0.3.29
earthkit-regrid==0.5.1
cartopy

# flash-attn is required (the transformer processor uses sliding-window attention;
# PyTorch SDPA falls back to a dense O(N^2) kernel that OOMs on the O96 grid).
# It is installed separately because the wheel is torch/CUDA/Python-specific.
# For the pins above (torch 2.6, CUDA 12.x, cp311, cxx11-abi=FALSE) use:
#   pip install https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.4.post1/flash_attn-2.7.4.post1+cu12torch2.6cxx11abiFALSE-cp311-cp311-linux_x86_64.whl
